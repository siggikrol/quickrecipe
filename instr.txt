Continue development of the recipe app from its current state.

Do not redesign the application. Preserve the current simple, fast, recipe-first approach.

Priority now is to review the existing implementation and make sure the current functionality is solid.

Please do the following:

1. Review the entire current codebase and understand the existing structure before making changes.
2. Verify that the “keep screen awake” functionality works while a recipe is open, especially on iPhone/Safari/PWA. It should activate when viewing a recipe and release appropriately when leaving the recipe.
3. Review recipe formatting throughout the app.
    * Recipes should not have unnecessary explanatory text.
    * Ingredients belonging to a component should sit underneath a simple section header.
    * Example:
        CURD
        ingredient
        ingredient
        ingredient
        CAKE
        ingredient
        ingredient
    * Do not repeat “curd”, “filling”, etc. beside every ingredient.
    * Use spacing and section headers to create the hierarchy.
4. Check every existing recipe for consistent structure:
    * title
    * optional short description only when useful
    * ingredients
    * ingredient groups/section headers where required
    * concise instructions
    * temperature/time
    * servings/yield where applicable
5. Bread recipes:
    * Keep hydration percentage visible where relevant.
    * Allow hydration to be adjusted only on recipes where changing hydration actually makes sense.
    * When hydration changes, automatically recalculate the water/liquid quantity based on flour weight.
    * Do not change other ingredients incorrectly when hydration is adjusted.
6. Verify that the recently added recipes, including Bounty cake, Drømmekage and the bread recipes, are represented correctly and consistently.
7. Test the app on a mobile-sized viewport first. This is primarily a phone recipe app.
    * Recipe must be quick to open.
    * Ingredients must be readable while baking.
    * Important controls should be easy to use with one hand.
    * Avoid unnecessary menus, modals and clutter.
8. Check persistence/state:
    * hydration changes
    * recipe navigation
    * favourites or other existing user state
        Make sure nothing unexpectedly resets or corrupts recipe data.
9. Run the existing tests/build/lint tooling and fix any errors introduced by previous changes.
10. Do not add new frameworks, large dependencies, authentication, backend services or unnecessary architecture unless there is a clear existing requirement.

The product principle is:

“Open the app → find recipe → cook.”

This is deliberately not a recipe-content website. No stories, SEO text, popups, adverts or long explanations.

After reviewing the code, make the necessary fixes directly. Then give me:

* what you changed
* files changed
* any issues you found
* anything that still needs testing on a real iPhone